name: visualization
version: 1.0.0
kind: compute

dependencies:
  - python3
  - python3-yaml
  - python3-pip

install:
  - pip install pandas
  - pip install matplotlib

files:
  - brane_visualization.py

entrypoint:
  kind: task
  exec: brane_visualization.py

actions:
  'groupby_plot':
    command:
      args:
        - groupby_plot
    input:
      - type: string
        name: kind
      - type: string
        name: input_path
      - type: string
        name: file
      - type: string
        name: column_name
      - type: real
        name: threshold_others
      - type: string
        name: title
      - type: boolean
        name: drop_nan

    output:
      - type: string
        name: output